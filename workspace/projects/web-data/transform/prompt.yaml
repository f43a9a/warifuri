Please transform the extracted data located at ../extract/urls.txt into a clean JSON format.

Project context: web-data
Data source: website
Output format expected: Clean, structured JSON

Requirements:
1. Parse the input data from ../extract/urls.txt
2. Clean and normalize the data
3. Convert to structured JSON format
4. Save to transformed_data.json

The data comes from website and needs to be prepared for further processing in the web-data pipeline.
