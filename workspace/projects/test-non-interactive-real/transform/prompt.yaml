prompt: |
  Please transform the extracted data located at input.txt into a clean JSON format.

  Project context: test-non-interactive-real
  Data source: source_data
  Output format expected: Clean, structured JSON

  Requirements:
  1. Parse the input data from input.txt
  2. Clean and normalize the data
  3. Convert to structured JSON format
  4. Save to transformed_data.json

  The data comes from source_data and needs to be prepared for further processing in the test-non-interactive-real pipeline.
