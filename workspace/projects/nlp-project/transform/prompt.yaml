Please transform the extracted data located at ../extract/data.csv into a clean JSON format.

Project context: nlp-project
Data source: database
Output format expected: Clean, structured JSON

Requirements:
1. Parse the input data from ../extract/data.csv
2. Clean and normalize the data
3. Convert to structured JSON format
4. Save to transformed_data.json

The data comes from database and needs to be prepared for further processing in the nlp-project pipeline.
