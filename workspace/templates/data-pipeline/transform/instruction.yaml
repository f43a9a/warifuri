name: transform
task_type: ai
description: Transform extracted data using AI for {{PROJECT_NAME}}
auto_merge: false
outputs:
  - "transformed_data.json"
inputs:
  - "../extract/extracted_data.{{OUTPUT_FORMAT}}"
dependencies:
  - "extract"
note: "AI-powered data transformation for {{PROJECT_NAME}}"
